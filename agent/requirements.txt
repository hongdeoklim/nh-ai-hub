# NH-AI-HUB AutoCAD 로컬 에이전트 의존성
# Windows 전용 패키지 (pywin32)는 Linux/macOS에서 설치 불가 — 개발 환경 주의

pywin32>=306          # AutoCAD COM 제어 (Windows 전용)
supabase>=2.4.0       # cad_jobs 테이블 polling
requests>=2.31.0      # Edge Function 직접 호출 (Phase 6)
ezdxf>=1.3.0          # 외부 dwg 정적 스캔 (Phase 4)
psutil>=5.9.0         # AutoCAD 프로세스 PID 식별 (워치독 대상 한정)
